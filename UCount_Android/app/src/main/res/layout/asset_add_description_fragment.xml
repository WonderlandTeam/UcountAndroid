<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/main_bg"
    >

    <android.support.v7.widget.CardView
        android:id="@+id/asset_add_description_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        >

        <ImageView
            android:id="@+id/asset_add_description_back"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="8dp"
            android:clickable="true"
            android:src="@mipmap/ic_back3" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:gravity="center"
            android:text="备注"
            android:textSize="20sp" />
    </android.support.v7.widget.CardView>

    <at.markushi.ui.CircleButton
        android:id="@+id/asset_add_description_save"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@mipmap/ic_tick"
        app:cb_color="#99CC00"
        app:cb_pressedRingWidth="8dip"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="40dp"
        android:layout_centerHorizontal="true"
        >
    </at.markushi.ui.CircleButton>


    <EditText
        android:id="@+id/asset_add_description_et"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/asset_add_description_header"
        android:layout_above="@id/asset_add_description_save"
        android:layout_marginBottom="8dp"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        android:hint="输入备注"
        android:gravity="left"
        android:ems="30"
        android:textColorHint="#999999"
        >
        <requestFocus />
    </EditText>






</RelativeLayout>